  }
   if(missing.length<k){
    let start=arr[arr.length-1]+1
    }
        c++
        i++
    }else{
        c++
    while(missing.length!=k){
        missing.push(start)
        start++
    }
   }
   return missing[k-1]
};
        missing.push(c)
[
