/**
 * @param {number} millis
 * @return {Promise}
 */
async function sleep(millis) {
    return new Promise((res,rej)=>{
}

/** 
 * let t = Date.now()
        setTimeout(()=>{
    })
            res(`Function delayed for ${millis}`)
        },millis)
1
